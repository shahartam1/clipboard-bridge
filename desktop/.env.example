# Copy this file to .env and fill in your server's IP
# When running the server on your Mac and connecting from another device on the same Wi-Fi:
VITE_SERVER_URL=ws://192.168.1.116:8787

# When both devices are on the same machine (default):
# VITE_SERVER_URL=ws://localhost:8787
